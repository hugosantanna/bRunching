% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discontinuity.R
\name{discontinuity_plot}
\alias{discontinuity_plot}
\title{Discontinuity Plot at the Bunching Point}
\usage{
discontinuity_plot(
  outcome,
  treatment,
  data,
  controls = NULL,
  instrument = NULL,
  bandwidth = NULL,
  max_treatment = 8,
  bin_width = 0.1,
  residualize = FALSE
)
}
\arguments{
\item{outcome}{Character string. Name of the outcome variable in \code{data}.}

\item{treatment}{Character string. Name of the treatment variable in \code{data}.}

\item{data}{A data.frame.}

\item{controls}{Character vector of control variable names for
residualization. If \code{NULL}, uses raw outcome.}

\item{instrument}{Character string. Name of the instrument for
residualization (used in the conditioning set). Optional.}

\item{bandwidth}{Bandwidth for local linear regression. If \code{NULL}, uses
the default from \code{\link[stats:loess]{stats::loess()}}.}

\item{max_treatment}{Numeric. Maximum treatment value to include (default 8,
matching the Stata code).}

\item{bin_width}{Numeric. Width of bins for the scatter plot (default 0.1).}

\item{residualize}{Logical. If \code{TRUE} and \code{controls} is provided,
residualize the outcome on controls before plotting.}
}
\value{
A ggplot2 object.
}
\description{
Creates a plot showing the outcome mean at the bunching point (zero) versus
a local-linear extrapolation from positive treatment values, with confidence
intervals and a p-value for the discontinuity test.
}
\examples{
\dontrun{
set.seed(42)
n <- 5000
z <- runif(n, 0, 10)
x_star <- 2 + 0.5 * z + rnorm(n)
x <- pmax(x_star, 0)
y <- 1 + 0.8 * x_star + rnorm(n)
df <- data.frame(y = y, x = x, z = z)
discontinuity_plot("y", "x", df)
}

}
