% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cct.R
\name{cct_beta}
\alias{cct_beta}
\title{Algorithm B (linear form): compute beta(0+) via OLS on a generated regressor}
\usage{
cct_beta(Y, X, m_hat, controls = NULL)
}
\arguments{
\item{Y}{Outcome vector.}

\item{X}{Treatment vector.}

\item{m_hat}{Per-observation estimate of \eqn{E[X^\ast \mid Z_i]}.}

\item{controls}{Optional matrix of control variables.}
}
\value{
A list with \code{beta} (scalar), \code{pi_hat}, \code{Delta_over_pi},
\code{gamma0}, and \code{fit} (the underlying \code{lm}).
}
\description{
Under Assumption 7 (Proposition 5.1), beta(0+) is the coefficient on X
in \code{Y ~ 1 + X + Xtilde (+ controls)} where
\verb{Xtilde = X + 1\{X = 0\} * pi_hat} and
\code{pi_hat = sum(m_hat - X) / sum(X == 0)}.
}
\keyword{internal}
