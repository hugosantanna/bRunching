% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccn_bootstrap.R
\name{ccn_bootstrap}
\alias{ccn_bootstrap}
\title{Pairs bootstrap for the corrected CCN methods}
\usage{
ccn_bootstrap(
  S,
  I,
  Z,
  method,
  n_bins,
  controls_mat,
  het_beta_mat,
  het_delta_mat,
  cluster_delta,
  swap,
  treatment,
  beta_names,
  delta_names,
  B = 500L,
  seed = NULL
)
}
\arguments{
\item{S, I, Z}{Outcome, treatment, instrument vectors (full sample).}

\item{method}{One of \code{"uniform"}, \code{"tobit"}, \code{"het_tobit"}, \code{"symmetric"}.}

\item{n_bins}{Number of instrument bins (already resolved upstream).}

\item{controls_mat, het_beta_mat, het_delta_mat}{Numeric matrices (or
\code{NULL}) over the full sample.}

\item{cluster_delta, swap}{Pass-through arguments for \code{ccn_internal_fit}.}

\item{treatment}{Treatment variable name (for labelling beta).}

\item{beta_names, delta_names}{Coefficient names from the main fit; used
to size and label the bootstrap result matrices so even iterations
where the resample's design has different bin counts are aligned by
common position (extras are dropped, gaps filled with NA).}

\item{B}{Number of bootstrap replications.}

\item{seed}{Optional integer seed.}
}
\value{
A list with \code{beta_b}, \code{delta_b}, \code{se_beta}, \code{se_delta},
\code{ci_beta}, \code{ci_delta}, \code{B_ok}.
}
\description{
Re-runs \code{ccn_internal_fit()} on each resample. Returns SE and percentile
CIs for \code{beta} and \code{delta}.
}
\keyword{internal}
